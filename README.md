# Hierarchical Analytics Platform (HAP)

Интерактивная платформа для управления и визуализации иерархических данных (регионы, города, объекты) с использованием **Laravel 11** и паттерна **Closure Table**.

## 🚀 Особенности проекта

* **Closure Table Pattern**: Эффективное хранение и поиск деревьев любой вложенности.
* **Интерактивный Sunburst Chart**: Визуализация данных в виде круговой диаграммы с поддержкой Drill-down (погружения).
* **RESTful API**: Полный цикл CRUD для управления узлами.
* **Modern UI**: Интерфейс на TailwindCSS с динамической подгрузкой данных через Axios.

## 🛠 Технологический стек

* **Backend**: Laravel 11 (PHP 8.2+)
* **Database**: MySQL (с поддержкой внешних ключей)
* **Frontend**: JavaScript, Apache ECharts, TailwindCSS.

## 📦 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/afruzcoder/visualgo.git](https://github.com/afruzcoder/visualgo.git)
    cd visualgo
    ```

2.  **Установите зависимости:**
    ```bash
    composer install
    ```

3.  **Настройте окружение:**
    ```bash
    cp .env.example .env
    # Настройте подключение к БД в .env
    ```

4.  **Подготовьте приложение:**
    ```bash
    php artisan key:generate
    php artisan migrate
    php artisan install:api
    ```

5.  **Запустите сервер:**
    ```bash
    php artisan serve
    ```

## 📊 Как пользоваться

1.  **Создание**: Введите название и значение в форму справа. Если ничего не выбрано, узел станет корневым.
2.  **Навигация**: Кликните на сектор графика, чтобы выбрать его. Теперь новые узлы будут добавляться внутрь него.
3.  **Сброс**: Используйте кнопку "🏠 В корень", чтобы вернуться на верхний уровень.

---
*Разработано в рамках учебного проекта по структурам данных.*
